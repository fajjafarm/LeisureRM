@extends("layouts.osen") @section("title","NOP / EAP") @section("content") <div class="row">@foreach(\App\Models\NopEap::all() as $doc)<div class="col-md-4"><div class="card"><div class="card-body"><h5>{{$doc->title}}</h5>@if($doc->youtube_url)<iframe src="{{$doc->youtube_url}}" class="w-100" height="200"></iframe>@endif <a href="{{asset("storage/".$doc->pdf_file)}}" class="btn btn-primary mt-2">View PDF</a></div></div></div>@endforeach</div> @endsection
